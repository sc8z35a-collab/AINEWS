const SHELL_CACHE='ai-brief-shell-v2';
const DATA_CACHE='ai-brief-data-v2';
const SHELL=['/','/index.html','/trending.html','/openai.html','/google.html','/anthropic.html','/company.html','/article.html','/about.html','/styles.css','/common.js','/home.js','/company.js','/trending.js','/article.js','/manifest.webmanifest'];
self.addEventListener('install',event=>{event.waitUntil(caches.open(SHELL_CACHE).then(cache=>cache.addAll(SHELL)).then(()=>self.skipWaiting()))});
self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>![SHELL_CACHE,DATA_CACHE].includes(k)).map(k=>caches.delete(k)))).then(()=>self.clients.claim()))});
async function cacheIfOk(cacheName,request,response){if(response&&response.ok){const cache=await caches.open(cacheName);await cache.put(request,response.clone())}return response}
async function apiNetworkFirst(request){try{const response=await fetch(request);if(response.ok)await cacheIfOk(DATA_CACHE,request,response);return response}catch{const cached=await caches.match(request);if(cached)return cached;return new Response(JSON.stringify({error:'offline_cache_miss'}),{status:503,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store'}})}}
async function navigationNetworkFirst(request,url){try{const response=await fetch(request);if(response.ok)await cacheIfOk(SHELL_CACHE,request,response);return response}catch{const exact=await caches.match(request);if(exact)return exact;const shellPage=await caches.match(url.pathname);if(shellPage)return shellPage;return caches.match('/index.html')}}
async function assetStaleWhileRevalidate(event,request){const cached=await caches.match(request);const network=fetch(request).then(async response=>{if(response.ok)await cacheIfOk(SHELL_CACHE,request,response);return response}).catch(()=>null);if(cached){event.waitUntil(network);return cached}const response=await network;return response||new Response('Offline',{status:503,headers:{'content-type':'text/plain; charset=utf-8'}})}
self.addEventListener('fetch',event=>{const request=event.request;if(request.method!=='GET')return;const url=new URL(request.url);if(url.origin!==location.origin)return;if(url.pathname.startsWith('/api/')){event.respondWith(apiNetworkFirst(request));return}if(request.mode==='navigate'||request.destination==='document'){event.respondWith(navigationNetworkFirst(request,url));return}event.respondWith(assetStaleWhileRevalidate(event,request))});
