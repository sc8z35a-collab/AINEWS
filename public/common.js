const CATEGORY_LABELS={models:'モデル / 製品',infrastructure:'インフラ',safety:'安全性 / 政策',research:'研究',business:'企業 / 市場',general:'総合'};
const COMPANY_LABELS={openai:'OpenAI',google:'Google / DeepMind',anthropic:'Anthropic',nvidia:'NVIDIA',meta:'Meta',microsoft:'Microsoft',xai:'xAI',amazon:'Amazon / AWS'};
const $=(s,r=document)=>r.querySelector(s); const $$=(s,r=document)=>[...r.querySelectorAll(s)];
function esc(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function fmtDate(iso,{time=false}={}){try{return new Intl.DateTimeFormat('ja-JP',time?{month:'numeric',day:'numeric',hour:'2-digit',minute:'2-digit'}:{year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date(iso))}catch{return iso||''}}
function ago(iso){const d=(Date.now()-new Date(iso))/1000;if(d<0)return fmtDate(iso);if(d<3600)return `${Math.max(1,Math.floor(d/60))}分前`;if(d<86400)return `${Math.floor(d/3600)}時間前`;if(d<604800)return `${Math.floor(d/86400)}日前`;return fmtDate(iso)}
function trim(s='',n=170){s=String(s).replace(/\s+/g,' ').trim();return s.length>n?s.slice(0,n-1)+'…':s}
function primaryCategory(a){return a.categories?.[0]||'general'}
function labelClass(cat){return cat==='infrastructure'||cat==='business'?'rust':cat==='research'?'olive':''}
function articleHref(id){return `article.html?id=${encodeURIComponent(id)}`}
function companyHref(c){return ['openai','google','anthropic'].includes(c)?`${c}.html`:`company.html?company=${encodeURIComponent(c)}`}
function cardHTML(a){const cat=primaryCategory(a);const flags=[...(a.companies||[]).slice(0,2).map(x=>COMPANY_LABELS[x]||x),...(a.categories||[]).slice(0,2).map(x=>CATEGORY_LABELS[x]||x)].slice(0,3);return `<article class="news-card"><div class="card-top"><span class="label ${labelClass(cat)}">${esc(CATEGORY_LABELS[cat]||cat)}</span><span class="date">${esc(ago(a.publishedAt))}</span></div><h2>${esc(a.title)}</h2><p>${esc(trim(a.summary||'記事タイトルと配信元の情報をもとに掲載しています。詳細は記事ページから原文をご確認ください。'))}</p><div class="card-flags">${flags.map(x=>`<span class="tiny">${esc(x)}</span>`).join('')}</div><div class="card-foot"><span class="source">${esc(a.source)}</span><a class="detail-link" href="${articleHref(a.id)}">詳細を見る →</a></div></article>`}
function skeletons(n=6){return Array.from({length:n},()=>`<div class="sk-card"><div class="skeleton sk-line short"></div><div class="skeleton sk-line big"></div><div class="skeleton sk-line mid"></div><div class="skeleton sk-line"></div><div class="skeleton sk-line mid"></div></div>`).join('')}
function modeLabel(mode){return mode==='live'?'LIVE':mode==='stale-cache'?'CACHE':'FALLBACK'}
function updateGlobalStatus(meta){const dot=$('#statusDot'),text=$('#statusText'),updated=$('#globalUpdated');if(!meta)return;if(dot)dot.classList.toggle('warn',meta.mode!=='live'||meta.degraded);if(text)text.textContent=meta.mode==='live'?'自動取得：正常':meta.mode==='stale-cache'?'自動取得：キャッシュ表示':'自動取得：フォールバック表示';if(updated)updated.textContent=`最終取得 ${fmtDate(meta.generatedAt,{time:true})}`}
function toast(msg){let t=$('#toast');if(!t){t=document.createElement('div');t.id='toast';t.className='toast';document.body.append(t)}t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2200)}
async function api(path,opts={}){const res=await fetch(path,{headers:{accept:'application/json'},...opts});if(!res.ok)throw new Error(`${res.status} ${res.statusText}`);return res.json()}
function setupNav(){const btn=$('#menuBtn'),nav=$('#navLinks');btn?.addEventListener('click',()=>{const open=nav.classList.toggle('open');btn.setAttribute('aria-expanded',String(open))});const refresh=$('#refreshBtn');refresh?.addEventListener('click',async()=>{refresh.disabled=true;refresh.textContent='更新中…';try{await api('/api/news?force=1&limit=1');toast('最新ニュースを再取得しました');location.reload()}catch(e){toast('更新に失敗しました')}finally{refresh.disabled=false;refresh.textContent='↻ 更新'}})}
function companyFromQuery(){const q=new URLSearchParams(location.search).get('company');if(q)return q;const f=location.pathname.split('/').pop().replace('.html','');return ['openai','google','anthropic'].includes(f)?f:''}
document.addEventListener('DOMContentLoaded',setupNav);
