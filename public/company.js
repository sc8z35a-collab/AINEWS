'use strict';

const COMPANY_INFO={openai:{name:'OpenAI',desc:'ChatGPT、GPT、Codex、研究、安全性、企業導入、独自インフラなどOpenAI関連ニュース。'},google:{name:'Google / DeepMind',desc:'Gemini、Google DeepMind、Gemma、ロボティクス、科学研究、安全性などGoogleのAI関連ニュース。'},anthropic:{name:'Anthropic',desc:'Claude、エージェント、研究、安全性、企業導入などAnthropic関連ニュース。'},nvidia:{name:'NVIDIA',desc:'GPU、CUDA、Blackwell、Rubin、AIデータセンターなどNVIDIA関連ニュース。'},meta:{name:'Meta',desc:'Llama、Meta AI、研究、製品、安全性、インフラなどMeta関連ニュース。'},microsoft:{name:'Microsoft',desc:'Copilot、Azure AI、OpenAI連携、企業導入、インフラなどMicrosoft関連ニュース。'},xai:{name:'xAI',desc:'Grok、モデル、研究、製品、インフラなどxAI関連ニュース。'},amazon:{name:'Amazon / AWS',desc:'AWS、Bedrock、クラウドAI、企業導入、インフラなどAmazon関連ニュース。'}};
let rows=[],visibleCount=30,renderTimer;const key=companyFromQuery(),info=COMPANY_INFO[key]||null,stateKey=`ainews:company:${key}`;
function showInvalidCompany(){document.title='企業が見つかりません — AI BRIEF Ultra';if($('#companyTitle'))$('#companyTitle').textContent='企業が見つかりません';if($('#companyDesc'))$('#companyDesc').textContent='URLのcompany指定が不正、または未対応です。トップページから企業を選び直してください。';if($('#companyBadge'))$('#companyBadge').textContent='INVALID COMPANY';if($('#newsGrid'))$('#newsGrid').innerHTML='<div class="errorbox"><strong>企業指定が不正です</strong><a href="index.html">トップページへ戻る</a></div>'}
if(info){document.title=`${info.name} 最新AIニュース — AI BRIEF Ultra`;$('#companyTitle').textContent=info.name;$('#companyDesc').textContent=info.desc;$$('#navLinks a').forEach(a=>{if(a.getAttribute('href')===`${key}.html`)a.classList.add('active')})}else showInvalidCompany();
function saveState(){sessionStorage.setItem(stateKey,JSON.stringify({q:$('#searchInput')?.value||'',sort:$('#sortSelect')?.value||'latest',visibleCount,scrollY}))}
function restoreState(){let state={};try{state=JSON.parse(sessionStorage.getItem(stateKey)||'{}')}catch{}if($('#searchInput'))$('#searchInput').value=state.q||'';if($('#sortSelect'))$('#sortSelect').value=state.sort==='source'?'source':'latest';visibleCount=Math.max(30,Math.min(180,Number(state.visibleCount)||30));return Math.max(0,Number(state.scrollY)||0)}
function render(){if(!info)return;const q=($('#searchInput')?.value||'').trim().toLowerCase();const show=rows.filter(a=>!q||`${a.title||''} ${a.summary||''} ${a.source||''}`.toLowerCase().includes(q));if($('#sortSelect')?.value==='source')show.sort((a,b)=>String(a.source||'').localeCompare(String(b.source||''),'ja')||new Date(b.publishedAt||0)-new Date(a.publishedAt||0));else show.sort((a,b)=>new Date(b.publishedAt||0)-new Date(a.publishedAt||0));const shown=show.slice(0,visibleCount);$('#resultCount').textContent=`${show.length}件中 ${shown.length}件`;$('#newsGrid').innerHTML=shown.length?shown.map(cardHTML).join(''):'<div class="empty">現在、この企業に一致するニュースがありません。</div>';$('#loadMoreBtn')?.remove();if(shown.length<show.length){$('#newsGrid').insertAdjacentHTML('afterend',`<button id="loadMoreBtn" class="load-more" type="button">さらに表示（残り${show.length-shown.length}件）</button>`);$('#loadMoreBtn').addEventListener('click',()=>{visibleCount=Math.min(show.length,visibleCount+30);render()})}saveState()}
async function init(){if(!info)return;const restoreY=restoreState(),grid=$('#newsGrid');grid.innerHTML=skeletons(6);try{const data=await api(`/api/news?company=${encodeURIComponent(key)}&limit=180`);rows=Array.isArray(data.items)?data.items:[];updateGlobalStatus(data.meta);$('#companyBadge').textContent=`${modeLabel(data.meta?.mode)} · ${data.total??rows.length} ARTICLES`;render();requestAnimationFrame(()=>scrollTo({top:restoreY,behavior:'instant'}))}catch(e){grid.innerHTML='<div class="errorbox"><strong>取得に失敗しました</strong>サーバーまたはオフライン保存を確認してください。</div>'}}
$('#searchInput')?.addEventListener('input',()=>{clearTimeout(renderTimer);renderTimer=setTimeout(()=>{visibleCount=30;render()},160)});$('#sortSelect')?.addEventListener('change',()=>{visibleCount=30;render()});addEventListener('pagehide',saveState);init();
